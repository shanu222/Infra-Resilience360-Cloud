
  # Disaster Dashboard UX Flow

  This is a code bundle for Disaster Dashboard UX Flow. The original project is available at https://www.figma.com/design/uX6yy7RuNI5S3hWJ21tt3e/Disaster-Dashboard-UX-Flow.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  