/** Main application modules are now eagerly bundled for instant UI rendering. */
export function preloadSectionModules(): void {
  // Keep helper for compatibility with existing bootstrap flow.
  // No dynamic route imports are needed for core modules.
}
